cmake_minimum_required(VERSION 3.25.2)  # Specifies the required CMake version.
project(PracticeMechMorphogenesis)  # Defines the project name.
add_subdirectory(src) # Identifying directory where the actual code is.